import React from "react";

const FooterTitle = (title) => {
  return (
    <div className="footer-contacts-title">
      <h4 className="footer-title">{title.title}</h4>
    </div>
  );
};

export default FooterTitle;
