import React from "react";

const ConsultationTitle = (title) => {
  return (
    <div className="consultation-item-block-title">
      <h3>{title.title}</h3>
    </div>
  );
};

export default ConsultationTitle;
